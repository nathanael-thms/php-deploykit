#!/bin/bash

# Put custom commands here that you would like to run at the end of the script
# eg. since symfony does not support auto app down before deployment in classical mode, you may put your own logic to put the app back up here, if you had put it down
# Code here will be executed from the current directory(if symlink method is used) or the current app directory(if classical method is used), so be sure to cd to the correct directory if needed

