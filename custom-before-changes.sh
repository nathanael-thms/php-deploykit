#!/bin/bash

# Put custom commands here that you would like to run just before pulling the code
# eg. since symfony does not support auto app down before deployment in classical mode, you may put your own logic to put the app down here
# Code here will be executed from the release directory(if symlink method is used) or the current app directory(if classical method is used), so be sure to cd to the correct directory if needed

